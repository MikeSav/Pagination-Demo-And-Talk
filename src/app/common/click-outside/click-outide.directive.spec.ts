import { ClickOutideDirective } from './click-outide.directive';

describe('ClickOutideDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutideDirective();
    expect(directive).toBeTruthy();
  });
});
